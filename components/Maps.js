import React from 'react'
import { StyleSheet, Text, View } from 'react-native'
import { StyleSheet, Text, View } from 'react-native'


const Maps = () => {
    return (
    <MapView>

    </MapView>
    )
}

export default Maps

const styles = StyleSheet.create({})
